[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd backend && npm install", "cd backend && npx prisma generate"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && npm start" 