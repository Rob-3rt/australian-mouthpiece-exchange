{
  "name": "australian-mouthpiece-exchange",
  "version": "1.0.0",
  "description": "The Australian Mouthpiece Exchange - A marketplace for brass mouthpieces",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npx prisma generate",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 